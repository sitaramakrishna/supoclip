# Slim requirements for Vercel deployment (API layer only).
# Heavy video-processing deps (moviepy, opencv, whisper, mediapipe, etc.)
# run on the Railway worker — see worker/Dockerfile.
fastapi>=0.110.0
uvicorn>=0.27.0
sqlalchemy>=2.0.25
asyncpg>=0.29.0
alembic>=1.13.0
python-dotenv>=1.0.0
aiofiles>=23.2.0
sse-starlette>=1.6.5
arq>=0.26.0
redis>=5.0.0
resend>=2.13.0
requests>=2.32.0
httpx>=0.27.0
greenlet>=2.0.0
pydantic>=2.0.0
